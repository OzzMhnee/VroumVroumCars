<nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm">
	<div class="container">
		<a class="navbar-brand" href="{{ path('app_home_page') }}">
			<img src="/img/logoDark.png" alt="Logo" height="50" class="d-inline-block align-text-top"></a>
		<a class="navbar-brand fw-bold" href="{{ path('app_home_page') }}">VroumVroumCars</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarNav">
			<ul class="navbar-nav ms-auto mb-2 mb-lg-0">
				{% if brands is defined and brands|length > 0 %}
					<li class="nav-item dropdown">
						<a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">Marques</a>
						<ul class="dropdown-menu">
							<li>
								<a class="dropdown-item" href="{{ path('app_car_all') }}">Toutes les marques</a>
							</li>
							{% for brand in brands %}
								{% if brand.cars|length > 0 %}
									<hr class="dropdown-divider">
									<li>
										<a class="dropdown-item text-dark categoryTitle" href="{{ path('app_brand_show', {id: brand.id}) }}">{{ brand.name }}</a>
									</li>
									{% for car in brand.cars %}
										<li>
											<a class="dropdown-item text-secondary" href="{{ path('app_car_show', {id: car.id}) }}">
												{{ car.name }}</a>
										</li>
									{% endfor %}
								{% endif %}
							{% endfor %}
						</ul>
					</li>
				{% endif %}
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">Gestion des données</a>
					<ul class="dropdown-menu">
						<li>
							<a href="{{ path('app_brand') }}" class="dropdown-item">Marques</a>
						</li>
						<li>
							<a href="{{ path('app_brand_new') }}" class="dropdown-item">Ajouter une Marque</a>
						</li>
						<li><hr class="dropdown-divider"></li>
						<li>
							<a href="{{ path('app_car') }}" class="dropdown-item">Voitures</a>
						</li>
						<li>
							<a href="{{ path('app_car_new') }}" class="dropdown-item">Ajouter une Voiture</a>
						</li>
					</ul>
				</li>
			</ul>
		</div>
	</div>
</nav>
